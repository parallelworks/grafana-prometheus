apiVersion: 1

providers:
  - name: 'my-dashboards'
    orgId: 1
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
      # optional:
      # foldersFromFilesStructure: true